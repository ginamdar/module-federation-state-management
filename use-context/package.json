{
  "name": "use-context",
  "version": "1.0.0",
  "dependencies": {
    "learna": "^1.0.1"
  },
  "private": "true",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npx lerna run build --parallel"
  }
}
